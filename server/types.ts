export interface BookCategory {
    id: string,
    name: string
}

export interface BookReview {
    link: string,
    reviewer: string
}

export interface Book {
    title: string,
    author: string,
    isbn: string
}